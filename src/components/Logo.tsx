import "../css/Log.css";

const Logo = () => {
  return (
    <section
      style={{
        display: "flex",

        justifyContent: "center",
      }}
    >
      <p className="title-label">FINEbank.IO</p>
    </section>
  );
};

export default Logo;
